<Window x:Class="WebDavEncryptManager.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WebDAV 配置" Height="220" Width="380" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="服务器:" VerticalAlignment="Center" Grid.Row="0" Grid.Column="0" Margin="0,0,0,10"/>
        <TextBox x:Name="TxtUrl" Grid.Row="0" Grid.Column="1" Margin="0,0,0,10"/>

        <TextBlock Text="账 号:" VerticalAlignment="Center" Grid.Row="1" Grid.Column="0" Margin="0,0,0,10"/>
        <TextBox x:Name="TxtUser" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10"/>

        <TextBlock Text="密 码:" VerticalAlignment="Center" Grid.Row="2" Grid.Column="0" Margin="0,0,0,10"/>
        <PasswordBox x:Name="TxtPass" Grid.Row="2" Grid.Column="1" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <TextBlock x:Name="TxtStatus" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Button Content="验证并保存" Width="90" Height="28" Click="BtnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>