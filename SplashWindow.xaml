<Window x:Class="WebDavEncryptManager.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="启动中" Height="300" Width="500" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True">
    <Border CornerRadius="15" Background="White" BorderBrush="#007ACC" BorderThickness="1">
        <Grid>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="WebDAV 加密云同步系统" FontSize="24" FontWeight="Bold" Foreground="#333" Margin="0,0,0,10"/>

                <TextBlock x:Name="TxtStatus" Text="正在检查运行环境..." HorizontalAlignment="Center" Foreground="#666" Margin="0,10"/>

                <ProgressBar x:Name="PrgBar" Width="300" Height="10" IsIndeterminate="True" Margin="0,10" Foreground="#007ACC" Background="#EEE" BorderThickness="0"/>

                <TextBlock Text="© 2024 WebDavEncrypt Pro" FontSize="10" Foreground="#AAA" HorizontalAlignment="Center" Margin="0,20,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>